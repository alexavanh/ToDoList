<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="styles/main.css" />
    <title>Work Tasks</title>
  </head>
  <body>
    <%- include('partials/header.ejs') %>
    <h1 class="text-center">To Do List for Work!</h1>
    <br />
    <h2 class="text-center">Add your tasks!</h2>
    <form action="/work" method="post" class="text-center">
      <label for="wTask">Work Tasks:</label>
      <input type="text" id="wTask" name="wTask" />
      <input type="submit" value="Add!" />
    </form>
    <br />
    <h2 class="text-center">Work Tasks</h2>
    <ul class="list-group to-do-list">
      <% if (locals.wArr) { for (var i = 0; i < locals.wArr.length; i++) {%>
      <li>
        <input type="checkbox" class="strikethrough" name="wOff" />
        <label for="wOff" class="strikeThis"><%= wArr[i] %></label>
      </li>
      <% } } %>
    </ul>
  </body>
</html>
